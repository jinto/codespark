import SwiftUI

enum AppTheme {
    static let sidebarBackground = Color(nsColor: .init(red: 0.06, green: 0.06, blue: 0.08, alpha: 1))
    static let toolbarBackground = Color(nsColor: .init(red: 0.08, green: 0.08, blue: 0.10, alpha: 1))
    static let surfaceBackground = Color(nsColor: .init(red: 0.10, green: 0.10, blue: 0.12, alpha: 1))
    static let divider = Color.white.opacity(0.06)
    static let cardBackground = Color.white.opacity(0.04)
    static let accent = Color.purple
}
