import SwiftUI

// Placeholder: closed session restoration removed in project simplification (Phase 1+2).
// This file will be deleted in Phase 3.
struct RecentlyClosedSessionCardView: View {
    var body: some View {
        EmptyView()
    }
}
