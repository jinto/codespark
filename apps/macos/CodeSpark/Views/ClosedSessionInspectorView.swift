import SwiftUI

// Placeholder: closed session inspector removed in project simplification (Phase 1+2).
// This file will be deleted in Phase 3.
struct ClosedSessionInspectorView: View {
    var body: some View {
        EmptyView()
    }
}
