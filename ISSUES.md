- 배경 탭 24개가 계속 렌더링 → 배터리. `ghostty_surface_set_focus(surface, false)`가
  한 번도 포커스 안 받은 탭에는 영영 안 간다. → docs/2026-09-15-idle-tab-battery-drain.md
- 복원된 ssh 탭의 **이전 화면 텍스트가 `ssh` 프로세스 인자에 그대로 실린다**
  (`RestoredScreenReplay.inlineCommand` → `printf '%b' '…'`). 로컬 `ssh`/`login` 프로세스가
  **탭이 살아 있는 내내** 그 인자를 들고 있어 `ps`로 읽힌다 — 화면에 토큰·비밀번호가 떠 있었다면
  그대로 남는다. 2026-09-15 `pgrep -ilf codespark` 출력에 대화 화면 전체가 찍혀서 발견.
  원격의 `/bin/sh -c`도 잠깐 같은 인자를 갖는다(`exec`로 셸이 되며 사라짐).
  다른 사용자 계정에서도 보이는지는 미확인.
- 같은 인자의 **길이** — ARG_MAX는 1MB(`getconf ARG_MAX`). payload는 스크롤백이 아니라
  뷰포트뿐이라(`payload(for:)`) 200×60 한글 화면이 이스케이프 포함 수십 KB 수준 — 당장 넘을
  일은 낮다. 다만 크기 상한이 코드에 없고, 같은 인자에 terminfo base64(~10KB)와 접속 스크립트가
  함께 실린다. 위 항목을 고칠 때(파일/stdin으로 옮기면) 같이 사라진다.

<!-- 2026-08-31 확인하고 지움 -->
<!-- - inactive ws 들은 ... 같은 거로 줄여서 한줄에 표시하고 몇개인지 숫자를 표시하면 어떨까.
       → 967771b에서 구현됨. 탭 없는 워크트리가 `·· N more`로 접힌다. -->

<!-- 2026-08-21 확인하고 지움 -->
<!-- - Shift 키 눌러도 대문자 입력 안됨.  → 지금은 됨 -->
<!-- - Cmd-V 가 안먹어요.                → 지금은 됨 -->
